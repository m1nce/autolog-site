# autolog-site

Marketing and support site for AutoLog, the iOS trip logger that reads the car and never writes to it. Astro, static output, no analytics, fonts self-hosted.

`npm install && npm run dev` to work on it. Pushing to `main` deploys to GitHub Pages via `.github/workflows/deploy.yml`.
